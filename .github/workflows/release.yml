name: Release

# Publishes the package to npm via OIDC trusted publishing when a `v*` tag
# is pushed. No NPM_TOKEN required.
#
# Trusted publishing: register this workflow on npmjs.com under the
# `@o1-labs/mina-archive-sdk` package settings — Trusted Publisher → GitHub
# Actions → repository `o1-labs/mina-archive-sdk-js`, workflow `release.yml`,
# environment `npm`. Once registered, the OIDC `id-token` claim authenticates
# the publish step.
#
# Release flow:
#   - bump version in package.json
#   - git tag vX.Y.Z && git push origin vX.Y.Z
#
# Workflow refuses to publish if the tag doesn't match package.json.

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: write # for `gh release create`
      id-token: write # for OIDC trusted publishing
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20.18.0'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      # OIDC trusted publishing (the OIDC -> npm token exchange) requires
      # npm >= 11.5.1. Node 20 ships npm 10.x, which signs provenance via
      # GitHub OIDC but cannot authenticate the publish itself, so the
      # registry rejects the PUT with a 404. Upgrade before publishing.
      #
      # Pin the major instead of tracking `latest`: npm 12 raised its Node
      # floor to ^22.22.2 || ^24.15.0 || >=26.0.0, so `npm@latest` fails with
      # EBADENGINE against the Node version pinned above. npm 11 accepts
      # ^20.17.0 || >=22.9.0. This matches Archive-Node-API's publish job.
      - run: npm install -g npm@11
      - run: npm --version

      - run: npm ci
      - run: npm run build
      - run: npm run test:unit

      - name: Verify tag matches package.json version
        run: |
          tag="${GITHUB_REF_NAME#v}"
          pkg=$(node -e "console.log(require('./package.json').version)")
          if [ "$tag" != "$pkg" ]; then
            echo "tag $GITHUB_REF_NAME does not match package.json $pkg"; exit 1
          fi

      - name: Publish to npm
        run: npm publish --provenance --access public

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create "${{ github.ref_name }}" --generate-notes
